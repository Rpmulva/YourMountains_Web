import { Colors } from '../constants/theme';

const config = {
    colors: {
        // Claire Brand
        primary: Colors.claire.primary,
        claireOrange: Colors.claire.primary,
        
        // Community Colors
        communityGreen: Colors.community.green,
        enablementBlue: Colors.community.blue,
        exploringGold: Colors.community.gold,
        
        // UI Colors
        hickory: Colors.ui.surface,
        background: Colors.ui.background,
        card: Colors.ui.surface,
        text: Colors.ui.text,
        textSec: Colors.ui.textSecondary,
    }
};

export default config;